# 复制为 .env 并填写：cp .env.example .env
# 共享密钥：页面登录用。生成一个长随机串：openssl rand -hex 24
APP_TOKEN=

# 监听端口与地址（nginx 反代到这里，保持只监听本机）
PORT=3000
HOST=127.0.0.1

# 允许跨域的前端来源（GitHub Pages 域名）
ALLOW_ORIGIN=https://tools.gimago.cn
